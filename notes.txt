# list of apt packages to install and other stuff

1. mkdir Dev

- for spotify 
	sudo sh -c 'echo "deb http://repository.spotify.com stable non-free" >> /etc/apt/sources.list.d/spotify.list'

	sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 94558F59

2. Apt packages 

3. apt-get purge pidgeon thunderbird brasero

4. change firefoxs in about:config - layout.css.devPixelsPerPx

5. Install grafana http://docs.grafana.org/installation/debian/

6. Add udev rules for android usb devices -> $ wget -S -O - http://source.android.com/source/51-android.rules | sed "s/<username>/$USER/" | sudo tee >/dev/null /etc/udev/rules.d/51-android.rules; sudo udevadm control --reload-rules

7. Add Vundle to vim

8. Add SSD optimization options to fstab entry



#ADD
#/home/rvazquez/.config/sublime-text-3/Packages/User/C++11.sublime-build

# Install with apt this is for i3 stuff


# Install for i3 stuff
# sudo apt install -qq  rofi

# ln -sv -f ${BASEDIR}/i3Pystatus/config.py --> can run directly from dotfiles dir

# Add UDEV
# Android udev rules wget -S -O - http://source.android.com/source/51-android.rules | sed "s/<username>/$USER/" | sudo tee >/dev/null /etc/udev/rules.d/51-android.rules; sudo udevadm control --reload-rules


References:
[7] - https://github.com/VundleVim/Vundle.vim
